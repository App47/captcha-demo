<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title><%= content_for?(:page_title) ? yield(:page_title) : "Reset Demo" %></title>
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <!-- Bootstrap CSS via CDN (no build step required) -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />
  <style>
      body { padding-top: 2rem; }
  </style>
</head>
<body>
<nav class="navbar navbar-expand-lg navbar-light bg-light border-bottom">
  <div class="container">
    <a class="navbar-brand" href="/">Reset Demo</a>
  </div>
</nav>
<!-- Bootstrap Icons sprite (only symbols we use) -->
<svg xmlns="http://www.w3.org/2000/svg" style="display:none;">
  <symbol id="info-fill" viewBox="0 0 16 16" fill="currentColor">
    <path d="M8 0a8 8 0 1 0 0 16A8 8 0 0 0 8 0zM8.93 6.588l-2.29.287-.082.38.45.083c.294.07.352.176.288.469L6.2 11.5c-.194.897.105 1.319.808 1.319.545 0 1.178-.252 1.465-.598l.088-.416c-.2.176-.492.246-.686.246-.275 0-.377-.176-.304-.51l.738-3.468c.07-.34.012-.466-.3-.52zm-.81-2.29a1 1 0 1 0 0 2 1 1 0 0 0 0-2z"/>
  </symbol>
  <symbol id="check-circle-fill" viewBox="0 0 16 16" fill="currentColor">
    <path d="M16 8A8 8 0 1 1 0 8a8 8 0 0 1 16 0zM6.97 11.03a.75.75 0 0 0 1.08.02l3.992-3.99a.75.75 0 1 0-1.06-1.06L7.5 9.44 5.53 7.47a.75.75 0 0 0-1.06 1.06l2.5 2.5z"/>
  </symbol>
  <symbol id="exclamation-triangle-fill" viewBox="0 0 16 16" fill="currentColor">
    <path d="M8.982 1.566a1.13 1.13 0 0 0-1.964 0L.165 13.233c-.457.778.091 1.767.982 1.767h13.706c.89 0 1.438-.99.982-1.767L8.982 1.566zM8 5c.535 0 .954.462.9.995l-.35 3.507a.552.552 0 0 1-1.1 0L7.1 5.995A.905.905 0 0 1 8 5zm.002 6a1 1 0 1 0 .001 2 1 1 0 0 0-.001-2z"/>
  </symbol>
</svg>

<main class="container">
  <% flash.each do |type, messages| %>
    <% msgs = Array(messages).compact.reject(&:blank?) %>
    <% next if msgs.empty? %>

    <% css = case type.to_s
             when "info"   then "alert alert-primary"
             when "notice" then "alert alert-success"
             when "alert"  then "alert alert-danger"
             else               "alert alert-warning"
             end %>

    <% icon = case type.to_s
             when "info"   then "info-fill"
             when "notice" then "check-circle-fill"
             when "alert"  then "exclamation-triangle-fill"
             else               "exclamation-triangle-fill"
             end %>

    <% msgs.each do |msg| %>
      <div class="<%= css %> d-flex align-items-center mt-2" role="alert">
        <svg class="bi flex-shrink-0 me-2" width="20" height="20" role="img" aria-hidden="true">
          <use xlink:href="##{icon}"></use>
        </svg>
        <div><%= msg %></div>
      </div>
    <% end %>
  <% end %>

  <%= yield %>
</main>

<!-- Bootstrap JS bundle (with Popper) -->
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
<script src="https://captcha-dist.app47.net/widget.min.js"></script>
</body>
</html>